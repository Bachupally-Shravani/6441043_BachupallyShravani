$(document).ready(function () {
    $('#registerBtn').click(function () {
        alert('Registered via jQuery!');
    });

    $('.event-card').fadeIn(1000).fadeOut(1000);
});

// Advantage of React/Vue:
// Components are reusable, reactive, and easier to manage with larger apps.
