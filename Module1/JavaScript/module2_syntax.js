const eventName = "Art Fair";
const eventDate = "2025-06-20";
let availableSeats = 30;

console.log(`${eventName} is scheduled for ${eventDate}. Available seats: ${availableSeats}`);

availableSeats--;
console.log(`After one registration, seats left: ${availableSeats}`);
